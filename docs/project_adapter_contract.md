# Project adapter contract

An adapter is self-contained: its own `AGENTS.md`, `.codex`, helper, schemas,
ledger, roadmap, and log. Core-managed text is delimited by markers; adapter
rules remain outside them. v0.1 does not write those sections during sync.
