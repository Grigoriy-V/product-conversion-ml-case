# Lesson promotion

Local issue -> adapter report -> candidate rule -> human review -> Core release
-> dry-run sync -> adapter validation. Classify every rule as Core,
adapter-local, or private-never-sync. Private context is never promoted.
