# Bootstrap report

Created from Core v0.1 as a self-contained `classical_ml` adapter. No data,
model, training, evaluation, benchmark, download, or network operation ran.
The Core pin is `0.1.0`; the target has no symlink or global runtime dependency.
